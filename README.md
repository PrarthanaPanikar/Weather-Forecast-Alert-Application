# Weather-Forecast-Alert-Application
Build a system that fetches real-time and forecast weather for chosen cities/locations, computes risk alerts (rain, heat, wind, AQI if available), serves results via a FastAPI API, and visualizes maps/charts and notifications in a Next.js (or Streamlit) dashboard.
